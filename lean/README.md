# Lean 4 formalization

Target: mathlib-compatible statements of the completely-bounded Crouzeix conjecture and related special-class results.

## Current skeleton
See `CrouzeixCB.lean` for the main open statement and a placeholder for the first special class.

## Build
```bash
lake build
```
(Requires a standard mathlib setup; add `lakefile.lean` / `lake-manifest` as the formalization matures.)
